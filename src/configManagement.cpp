#include <toml++/toml.h>
#include <iostream>

int mkDefault(){
	/*This function will generate default configs in a .toml fileso that the program can
	 *use those defaults when loading the program first.

	 as soon as I figure out the lib*/

	return 0;
}

